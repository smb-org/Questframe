# Design: Win-Challenges als eingebettete Feature-Slice

Generated by /office-hours on 2026-08-30
Reviewed by /plan-eng-review on 2026-08-30
Branch: worktree-audit-broadcast
Repo: twitchBrudi (irl-stream-hud)
Status: DRAFT
Mode: Builder
Revision: 5 (Office Hours + zwei Codex-Spec-Reviews + Eng-Review + Outside Voice)

## Problem Statement

Streamer wollen während des Streams selbst formulierte Challenges anzeigen: optional mit
akt/soll-Zähler, optional mit Timer, abhakbar, mit sichtbarer Reaktion im Overlay.

Zwei Zusatzforderungen prägen den Entwurf mehr als das Feature selbst:

- Es soll ein **Modul** sein, das sich später in andere Produkte heben lässt.
- Es darf **nicht** in die rechte `editor-rail` der Admin-Konsole, die schon zu lang ist.

## What Makes This Cool

**Eine Aktion, verschiedene Welten.** Dasselbe Abhaken wird im Quest-Log-Style zur
Zeremonie und in der neutralen Liste zu einem stillen Haken. Der Style entscheidet.

**Das Quest Log ist eine Zustandsmaschine, kein Skin.** Der Objective-Text blitzt auf, wenn
der Zähler hochgeht, fertige Einträge werden gold, ein laufender Timer wird unter einer
Minute rot.

**Der Streamer verlässt OBS nie.** Der Live-Modus läuft als OBS Custom Browser Dock.

## Constraints

- Cloudflare Worker plus ein SQLite Durable Object pro Kanal. **Cloudflare Free Tier ist
  eine harte Vorgabe.** Keine neuen Produkte: kein D1, kein R2, keine Queues.
- **Kein serverseitiger Tick und kein Durable-Object-Alarm.** Das Overlay tickt bereits
  clientseitig im Sekundentakt (`src/overlay/OverlayApp.tsx:86`).
- Der 64-KiB-Zustandsblob (`channel_state.state_json`) darf nicht belastet werden.
- **Harte Build-Gates** (`scripts/check-build-budgets.mjs`): Overlay-JS 120 KiB,
  Overlay-Transfer 1 MiB, Worker-Bundle 750 KiB, Admin-JS 250 KiB.
- Das Overlay parst Socket-Nachrichten ohne Zod (`src/overlay/wire.ts`), genau wegen des
  120-KiB-Gates.
- Open Source: keine Blizzard-Assets, Namen, Fonts oder Audio.
- Die rechte `editor-rail` ist voll und darf nicht wachsen.

## Premises

- **P1.** Challenges hängen nicht am globalen HUD-Revisionszähler.
- **P2.** Eigene Tabellen mit `wc_`-Prefix im selben Durable Object, aber **kein** zweiter
  allgemeiner Konflikt-Mechanismus. Live-Kommandos sind atomar und dedupliziert und können
  nie mit einem Konflikt scheitern. Konfliktprüfung nur bei Board- und Settings-Saves.
- **P3.** Kein generischer SQL-Port. Ein domänenspezifisches `ChallengeRepository` mit
  genau einer Implementierung für DO-SQLite.
- **P4.** Das Modul meldet Fakten und weiß nichts von Flash, Sound oder Animation. Der Host
  hält die Style-zu-Zeremonie-Registry, den Aus-Schalter und die Audio-Policy.
- **P5.** Kein Scrollen und kein Paging im Overlay. Im Admin darf gescrollt werden.

## Recommended Approach

### Modul-Grenze und Import-Regeln

```
src/modules/win-challenges/
  contracts/
    predicates.ts   Reine Prädikate. EINZIGE Quelle der Wahrheit für Feldregeln.
                    Kein Zod-Import. Vom Zod-Schema UND vom Overlay-Parser benutzt.
    schemas.ts      Zod-Schemas, gebaut ÜBER predicates.ts, nie über z.string().max().
    events.ts       Event-Typen.
  domain/           Reine Logik: Zustandsübergänge, Clamping, Timer-Ableitung,
                    Overflow-Auswahl, Merge-Regeln. Kein I/O, kein Zod.
  service/          Kommando-Service. Kennt nur ChallengeRepository, nie SQL.
  repository/       ChallengeRepository-Interface.
  adapters/         SqlStorageChallengeRepository. Einziger Ort mit Tabellenpräfix.
  ui/               ChallengeLog (Overlay), ChallengeBoard (Admin), ChallengeDock (Live).
  styles/           Vier CSS-Dateien über einer gemeinsamen Basis, dynamisch geladen.
```

**Kein Sammel-Index.** Ein Fabrik-Einstieg würde über einen einzigen Import die
Kommandoschicht, das Repository und Zod ins Overlay-Bundle ziehen.

**Import-Regel, maschinell durchgesetzt.** Eine `no-restricted-imports`-Regel in
`eslint.config.js` verbietet Overlay-Code den Zugriff auf `service/`, `repository/`,
`adapters/` und `contracts/schemas`. **Erlaubt** sind `ui/ChallengeLog`, `domain/` und
`contracts/predicates`. Ein Verstoß ist sofort rot im Editor, nicht erst im Build-Gate.

Aufbau in zwei Schichten:

```ts
const repository = createSqlStorageChallengeRepository({
  sql: ctx.storage.sql,
  tablePrefix: "wc_",   // V1: Konstante, nicht nach außen konfigurierbar.
});
const challenges = createWinChallenges({ repository, clock, emit });
```

**V1-Scope:** konstantes Präfix, vier Styles, kein Capability-Manifest, keine Plattform für
künftige Module.

**Extrahierbarkeit, ehrlich:** Code-Organisation, keine fast fertige Paketierung. Die
Outside Voice hat angemerkt, dass sieben Unterbereiche für einen einzigen Adapter und
hostgebundene UIs viel Schichtung sind. Das ist ein fairer Einwand. Die Modul-Grenze wurde
trotzdem zweimal bewusst so gewählt, weil sie das erklärte Ziel des Vorhabens ist.

### Ein Validator, zwei Aufrufer

Der Host hatte schon einmal einen Drift zwischen zwei handgeschriebenen Validatoren
(Learning `grapheme-vs-utf16-validator-drift`, 2026-08-29): Server zählte Grapheme,
`wire.ts` UTF-16-Codeeinheiten, bei identischem Maximum. Ein Titel mit Emoji passierte den
Server und wurde vom Overlay still verworfen, die OBS-Quelle bekam nie wieder Zustand.

`contracts/predicates.ts` macht diesen Drift strukturell unmöglich. Sowohl das Zod-Schema
als auch der handgeschriebene Overlay-Parser rufen dieselben Funktionen auf. Ein
Grenzwerttest fährt eine Tabelle durch **beide** Aufrufer und erzwingt identisches
Accept/Reject.

### Migrations-Ledger: der vorhandene

Kein eigener Ledger. Die `wc_`-Tabellen kommen als `MIGRATION_3` in den bestehenden
Migrator (`src/channel/migrations.ts`).

### Datenmodell

```sql
CREATE TABLE wc_meta (
  singleton          INTEGER PRIMARY KEY CHECK (singleton = 1),
  event_seq          INTEGER NOT NULL,  -- steigt NUR, wenn wirklich ein Event ausgeht
  board_revision     INTEGER NOT NULL,  -- Konflikt nur für Board-Saves
  settings_revision  INTEGER NOT NULL,  -- Konflikt nur für Settings-Saves, getrennt
  style_id           TEXT    NOT NULL,  -- ChallengeStyleId, bestimmt nur den AUFBAU
  theme_mode         TEXT    NOT NULL CHECK (theme_mode IN ('inherit', 'own')),
  surface_mode       TEXT    NOT NULL CHECK (surface_mode IN ('surface', 'bare')),
  header_title       TEXT    NOT NULL,  -- 1..24, Vorgabe "CHALLENGES"
  effects_enabled    INTEGER NOT NULL CHECK (effects_enabled IN (0, 1)),
  max_visible        INTEGER NOT NULL CHECK (max_visible BETWEEN 3 AND 10),
  -- Optionaler globaler Timer über alle Challenges
  global_timer_total_ms         INTEGER,  -- NULL = Feature aus
  global_timer_ends_at          TEXT,     -- absoluter ISO-Instant, NULL wenn nicht laufend
  global_timer_paused_remain_ms INTEGER   -- eingefrorene Restzeit, NULL wenn nicht pausiert
);

CREATE TABLE wc_challenges (
  id             TEXT PRIMARY KEY,   -- crypto.randomUUID(), serverseitig erzeugt
  -- Definition: NUR über den Board-Save änderbar
  title          TEXT NOT NULL,      -- 1..80, normalisiert
  description    TEXT,               -- 0..160, nullable
  target_count   INTEGER,            -- NULL oder 1..999
  timer_total_ms INTEGER,            -- NULL oder 10_000..21_600_000
  sort_order     INTEGER NOT NULL,   -- 0..N-1, lückenlos, serverseitig renormalisiert
  -- Laufzeit: NUR über Live-Kommandos änderbar
  current_count  INTEGER NOT NULL,   -- 0..999
  state          TEXT NOT NULL CHECK (state IN ('pending', 'active', 'done')),
  timer_ends_at  TEXT,               -- absoluter ISO-Instant, NULL wenn kein Timer
  completed_at   TEXT,
  created_at     TEXT NOT NULL,
  updated_at     TEXT NOT NULL
);

CREATE TABLE wc_commands (           -- Idempotenz, für ALLE Mutationen
  command_id   TEXT PRIMARY KEY,
  request_hash TEXT NOT NULL,
  created_at   TEXT NOT NULL
);

CREATE TABLE wc_dock_tokens (        -- nach dem Muster von overlay_tokens
  singleton             INTEGER PRIMARY KEY CHECK (singleton = 1),
  token_hash            TEXT NOT NULL,
  token_envelope        TEXT,
  fingerprint           TEXT NOT NULL,
  generation            INTEGER NOT NULL,
  request_id            TEXT NOT NULL,
  creating_session_hash TEXT NOT NULL,
  created_at            TEXT NOT NULL,
  last_used_at          TEXT
);
```

**Bewusst nicht enthalten:** kein `wc_events`, kein `wc_runs`, kein `run_id`, kein
`failed`-Zustand, kein `suppress_auto_complete`.

**"Abgelaufen" ist abgeleitet:**
`timer_ends_at !== null && now > timer_ends_at && state !== 'done'`.

Maximal 30 Challenges. Mehrere gleichzeitig `active` sind erlaubt.

### Zustandsübergänge

| von | Kommando | nach | Nebenwirkung |
|---|---|---|---|
| `pending` | `startTimer` | `active` | `timer_ends_at = now + timer_total_ms`, Event `timer_started` |
| `active` | `stopTimer` | `pending` | `timer_ends_at = NULL`, Event `timer_stopped` |
| `pending` \| `active` | `complete` | `done` | `completed_at`, Event `completed` |
| `pending` \| `active` | `increment` | unverändert, außer Auto-Complete | Event `progressed` |
| `done` | `reopen` | `active` falls `timer_ends_at` in der Zukunft, sonst `pending` | `completed_at = NULL`, Event `reopened` |

- **Auto-Complete wird ausschließlich innerhalb von `increment` ausgewertet.** `reopen` ist
  kein `increment`, kann also nicht sofort wieder zuschnappen. Keine Unterdrückungs-Spalte.
- **Clamping:** `delta` in `-99..99`, Ergebnis in `[0, target_count ?? 999]`.
- Erreicht `increment` das Ziel, feuert genau ein `completed`, **kein** `progressed`.
- **Kommando ohne Wirkung** erzeugt kein Event und erhöht `event_seq` nicht.
- **Abgelaufene `active`-Challenge:** alle Kommandos bleiben erlaubt. Ablauf ist eine reine
  Anzeigeeigenschaft, keine Sperre.
- Löschen, Umsortieren und Textänderungen sind **Board-Edits**, keine Live-Kommandos.

### Kommandomodell: alle Mutationen sind dedupliziert

Die frühere Annahme, `complete`, `reopen`, `startTimer` und `stopTimer` seien "von Natur
aus idempotent", ist falsch. Zielzustandsoperationen sind nur idempotent, solange dazwischen
nichts passiert. Die realen Gegenbeispiele:

- `complete` → `reopen` → verspäteter `complete`-Retry: vollendet ein zweites Mal.
- `startTimer` → `stopTimer` → verspäteter `startTimer`-Retry: startet einen neuen Timer
  mit neuem Endzeitpunkt.

**Deshalb trägt jede Mutation eine `commandId` (UUID) und wird über `wc_commands`
dedupliziert**, nicht nur `increment`.

`request_hash` ist SHA-256 über das validierte Kommando-DTO in kanonischer
Feldreihenfolge, nicht über rohe JSON-Bytes.

Bei Wiederholung mit gleichem `commandId`: gleicher Hash → `{ eventSeq, challenge, replayed: true }`
mit dem **aktuellen** Stand; abweichender Hash → `idempotency_mismatch`.

Summierung als atomares
`UPDATE wc_challenges SET current_count = MIN(MAX(current_count + ?, 0), ?) WHERE id = ?`
**innerhalb desselben `transactionSync`-Callbacks** wie der `wc_commands`-Insert und der
`event_seq`-Bump (Muster: `src/channel/channel-object.ts:585`).

**Auch bei Board- und Settings-Saves** müssen Lesen der Revision, Prüfung und Schreiben im
selben `transactionSync`-Callback liegen. "Der DO serialisiert alles" reicht nicht, weil an
`await`-Grenzen verschachtelt werden kann.

`wc_commands` wird nach dem Muster von `pruneHistoryAndAudit` beschnitten: älter als 24 h.

### Board-Save: Anlegen, Mergen, Löschen

**Anlegen.** Neue Einträge tragen keine `id`, sondern eine vom Client vergebene
`clientId` (beliebiger String, nur innerhalb dieses Requests eindeutig). Der Server erzeugt
die echte `id` per `crypto.randomUUID()` und liefert in der Antwort eine Zuordnung
`{ clientId → id }` mit. Ohne diese Zuordnung könnte der Client seinen neuen Eintrag nach
dem Save nicht wiederfinden.

**Mergen.** Die Board-API akzeptiert ausschließlich Definitionsfelder: `id` oder
`clientId`, `title`, `description`, `targetCount`, `timerTotalMs`, `sortOrder`.
Laufzeitfelder sind im Request-Schema nicht vorhanden. Blindes Erhalten reicht aber nicht,
diese vier Fälle sind explizit geregelt:

| Definitionsänderung | Wirkung auf die Laufzeit |
|---|---|
| `timerTotalMs` wird `null`, Timer läuft | `timer_ends_at = NULL`, `state` von `active` auf `pending`. Ein Timer ohne Definition darf nicht existieren. |
| `timerTotalMs` ändert sich, Timer läuft | `timer_ends_at` bleibt unangetastet. Der laufende Timer behält seinen Endzeitpunkt, die neue Dauer gilt ab dem nächsten `startTimer`. |
| `targetCount` wird `null` | `current_count` bleibt gespeichert, wird aber nicht mehr angezeigt. Wird `targetCount` später wieder gesetzt, taucht der alte Stand wieder auf. Das ist gewollt und muss in der Admin-UI sichtbar sein, sonst überrascht es. |
| `targetCount` sinkt unter `current_count` | `current_count` wird auf den neuen Wert geklemmt. Kein Event, kein Auto-Complete. |

**Löschen.** Ein Eintrag, der im Request fehlt, wird gelöscht. `sort_order` wird danach
serverseitig auf `0..N-1` renormalisiert.

Ein Board-Save kann einen Zähler nicht zurückdrehen.

### Konfliktantwort trägt den Zustand mit

`revision_conflict` bei Board- oder Settings-Saves antwortet **mit dem aktuellen Snapshot
im Body**. Sich auf den "ohnehin eintreffenden" Broadcast zu verlassen wäre falsch: Er kann
vor der 409-Antwort verarbeitet worden sein, verloren gehen oder wegen eines
Socket-Abbruchs nie ankommen, und nach einem Konflikt ist kein neuer Broadcast garantiert.

### Zeremonien sind flüchtig, es gibt kein Event-Log

**Eine atomare Nachricht:**

```ts
challenge_update {
  eventSeq: number,
  boardRevision: number,
  settingsRevision: number,
  settings: { styleId, effectsEnabled, maxVisible },
  challenges: Challenge[],
  event: null | {
    type: "progressed" | "completed" | "reopened" | "timer_started" | "timer_stopped",
    challengeId: string,
    delta?: number, previousCount?: number, currentCount?: number,   // nur progressed
  },
}
```

**Client-Regel:** Zeremonie feuern genau dann, wenn `event !== null` **und**
`eventSeq > lastSeen`. Danach `lastSeen = eventSeq`. Die erste Nachricht nach dem Verbinden
trägt `event: null` und setzt nur `lastSeen`.

**Bewusste Konsequenz:** Wer während einer Vollendung getrennt war, verpasst den Effekt.
Der Zustand ist danach korrekt. Aus demselben Grund gibt es keine Ablaufzeremonie.

### Fehlerverhalten im Overlay

Das Overlay hat für HUD-Nachrichten einen markergeschützten Watchdog, der bei Parse-Fehlern
die Seite einmal neu lädt (`src/overlay/OverlayApp.tsx:21`, `isStateBearingMessage`).

**`challenge_update` wird bewusst NICHT in `isStateBearingMessage` aufgenommen.** Eine
kaputte Challenge-Nachricht darf das HUD nicht vor Zuschauern neu laden.

- **Parse-Fehler:** nur das Challenge-Log wird ausgeblendet, das HUD bleibt unberührt. Die
  nächste gültige Nachricht bringt das Log zurück.
- **Verbindungsabbruch:** dasselbe sichtbare Verhalten.

Ein Reload des Overlays wegen eines Challenge-Fehlers ist ein Bug, kein Feature.

### Offline-Verhalten

Der Offline-Cache speichert unter `hud:2:<capsule>:<fingerprint>` ausschließlich den
`ChannelState` (`src/overlay/cache.ts:18`). **Challenges werden nicht gecacht.** Ein
veraltetes Quest Log mit falschen Zählern wäre schlechter als keines.

### Timer ohne serverseitigen Tick, und der akzeptierte Uhr-Drift

`timer_ends_at` ist ein absoluter, serverautoritativer ISO-Instant, wie das vorhandene
`activeEffect.expiresAt`. Der Client rechnet die Restzeit lokal.

**Der Uhr-Drift wird bewusst akzeptiert.** Das Overlay besitzt heute keinen Uhrabgleich: es
fordert `time_sync` nie an, sein Parser akzeptiert die Nachricht nicht, es rechnet mit
`Date.now()`. Effekt-Timer laufen schon heute mit dem Drift des OBS-Rechners.

**Kein Durable-Object-Alarm.** Alarme wecken das DO, Alarm-Invokationen zählen als Requests
und `setAlarm()` als Row-Write.

### API

Editor-Routen nutzen die bestehende Kette: Same-Origin, `x-csrf-token`, `x-editor-tab`.

| Route | Methode | Auth | Body | Fehler |
|---|---|---|---|---|
| `/api/challenges` | GET | Session | — | `unauthorized` |
| `/api/challenges/commands` | POST | Session **oder** Dock-Token | `{ commandId, type, challengeId, delta? }` | `validation_failed`, `idempotency_mismatch`, `not_found`, `rate_limited` |
| `/api/challenges/board` | PUT | Session | `{ baseBoardRevision, challenges: Definition[] }` | `revision_conflict` (mit Snapshot), `payload_too_large` |
| `/api/challenges/settings` | PUT | Session | `{ baseSettingsRevision, styleId, effectsEnabled, maxVisible }` | `revision_conflict` (mit Snapshot), `validation_failed` |
| `/api/challenges/dock-token` | POST | Session | `{ requestId, expectedGeneration }` | `idempotency_mismatch`, `token_changed` |
| `/api/challenges/dock-token/rotate` | POST | Session | `{ requestId, expectedGeneration }` | `token_changed` |

Body-Limit für den Board-Save: 32 KiB. Der Dock-Token-Status (Existenz, Generation,
Fingerprint, verbundene Sockets) kommt über die bestehende Bootstrap-Antwort, genau wie
`overlayToken` es heute tut.

### Der Dock braucht einen eigenen Socket

Ein Dock, der nur Kommandos senden kann, ist blind: Er bekäme keine Korrekturen, keine
parallelen Änderungen und keinen Widerruf mit, und das optimistische Rückrollen hätte
keine Wahrheitsquelle. Deshalb bekommt er einen eigenen Socket.

**`/ws/dock`**, exakt nach dem Muster von `/ws/overlay` (`channel-object.ts:976`):

- Token im Sec-WebSocket-Protocol, wie beim Overlay.
- Eigenes Socket-Tag `dock`, eigenes Verbindungslimit.
- Beim Verbinden geht sofort ein vollständiges `challenge_update` mit `event: null` raus.
- Bei Token-Rotation werden verbundene Dock-Sockets aktiv geschlossen, so wie es der
  Overlay-Token schon macht.

`challenge_update` geht damit an drei Socket-Arten: `editor`, `overlay` und `dock`, über
den vorhandenen `sendToSockets`-Helfer.

**Warum Token statt Session:** OAuth-Redirects zu Drittanbietern gelten im eingebetteten
CEF-Browser von OBS ausdrücklich als unzuverlässig, und der Dock hat ein eigenes
Cookie-Profil. Damit entfällt jede Änderung an den OAuth-Flüssen: kein signiertes
`returnTo`, keine Allowlist, keine Anpassung an `src/worker/index.ts:158` und `:295`.

**Sicherheitsgrenze.** Anders als der Overlay-Token darf dieser schreiben. Sein Scope ist
hart auf `/api/challenges/commands` und `/ws/dock` begrenzt. Board, Settings, HUD-State,
Token-Rotation und alle anderen Routen sind für ihn geschlossen. Das ist ein Testfall.

**Rate-Limit.** Ein dritter Limiter `DOCK_TOKEN_LIMITER` in `wrangler.jsonc` neben die
beiden vorhandenen, angewendet auf die Kommando-Route und den Dock-Socket-Upgrade, nach dem
Muster von `src/worker/index.ts:570-571`. Etwa 30 Anfragen pro 10 Sekunden. Ohne ihn könnte
ein geleakter Token das harte Tageskontingent von 100.000 DO-Requests verbrennen und damit
Overlay, Admin und Login gleichzeitig lahmlegen.

### Socket-Nachrichten und Decoder

- Admin und Server dekodieren über `serverMessageSchema` (Zod), wie heute.
- Overlay und Dock bekommen einen **handgeschriebenen Exact-Key-Parser** in
  `src/overlay/wire.ts`, gebaut über `contracts/predicates.ts`. Kein Zod in diesen Bundles.
- Nach der Umsetzung wird das 120-KiB-Gate gemessen, nicht geschätzt.

### Styles und Zeremonien

```ts
type ChallengeStyleId = "plain-list" | "plain-bullets" | "plain-numbered" | "quest-log";
type CeremonySpec = {
  overlayFlash: boolean;
  soundAssetId: string | null;
  banner: "none" | "quest-complete" | "objective";
  reducedMotion: "none" | "fade";
};
const CEREMONIES: Record<ChallengeStyleId, Partial<Record<ChallengeEventType, CeremonySpec>>>;
```

Die drei `plain-*`-Styles unterscheiden sich in genau einer CSS-Regel (`list-style-type`)
und teilen sich alles Übrige über eine gemeinsame Basis.

Default: `plain-list`. Ein Style ohne Registry-Eintrag macht nichts; kein
`if (style === ...)` im Code. `wc_meta.effects_enabled` ist der globale Aus-Schalter,
`prefers-reduced-motion` wird respektiert.

**Style-CSS wird dynamisch geladen.** Der Budget-Checker summiert alles CSS im statischen
Import-Closure (`scripts/check-build-budgets.mjs:35`). Nur wenn die vier Styles echte
dynamische Chunks sind, darf im Gate `Math.max` statt einer Summe stehen, so wie
`commonHudMedia` es für die Theme-Varianten tut. Sonst wäre das Budget schöngerechnet.
Der Preis ist ein kurzes Nachladen beim Stylewechsel.

**Audio:** Sound-Dateien liegen unter `public/` und landen über den Vite-Build in
`dist/client`, das als Static-Asset-Verzeichnis gebunden ist (`wrangler.jsonc:9`). Sie
zählen nicht ins Worker-Skript, und statische Asset-Requests sind kostenlos, solange sie
nicht über `run_worker_first` laufen (das trifft nur `/api/*`, `/auth/*`, `/ws/*`,
`/healthz`).

**Das Transfer-Gate muss erweitert werden.** Es zählt heute nur JS, CSS und HUD-Medien
(`scripts/check-build-budgets.mjs:87`); Audio läuft ungeprüft durch.

### Admin wird zur Shell

- Die Topbar bekommt einen Workspace-Switcher: `HUD | Challenges`.
- `/admin` bleibt das HUD mit der bestehenden `editor-rail`, unverändert.
- `/admin/challenges` wird ein eigener Vollbreiten-Workspace.
- V1 bringt genau diese zwei Workspaces.

### Live-Bedienseite

`/live/challenges` ist eine kompakte Bedienseite, **kein Overlay**. Sie geht nie in die
Ausgabe und Zuschauer sehen sie nie. Wie der Streamer sie öffnet, ist seine Wahl: als
Custom Browser Dock in OBS, auf dem Handy neben der Tastatur, oder in einem zweiten
Browserfenster. Der Code ist in allen drei Fällen identisch, es ist eine URL.

- Schmal, große Ziele: `+` und `−` sind 44 px, alle übrigen Bedienelemente 38 px, nach
  der Vorgabe aus `DESIGN.md`.
- Inhalt: Challenges abhaken und zählen, Challenge-Timer starten und stoppen, Rückgängig,
  plus **ein Umschalter für Start und Pause des globalen Timers**. Reset des globalen
  Timers liegt bewusst NICHT hier, sondern im Admin, weil er einen laufenden Countdown
  ohne Weg zurück löscht.
- Unter 280 px Breite zeigt die Seite nur die gepinnte Challenge und den Zähler. Alles
  andere entfällt, statt zu schrumpfen.
- `src/main.tsx` braucht dafür einen dritten Zweig; heute landet der Pfad im `AdminApp`.
- **Optimistische Bedienung mit Rückrollen.** Der Zähler springt sofort, die Zeile markiert
  sich als schwebend, das `challenge_update` über `/ws/dock` ist die Wahrheit und
  korrigiert. Bei einem Fehler fällt der Wert zurück und die Zeile zeigt kurz den Grund.
  Bei `not_found` steht dort, dass die Challenge gerade gelöscht wurde, bevor die Zeile
  verschwindet.
- **Beide Öffnungswege werden gleichwertig dokumentiert**, mit QR-Code für den Handy-Weg
  und dem Menüpfad View → Docks → Custom Browser Docks für den OBS-Weg. Am OBS-Weg steht
  der Hinweis, dass Browser-Docks unter Wayland nicht zur Verfügung stehen und dass der
  eingebettete Browser ein eigenes Cookie-Profil hat, weshalb die Seite per Token-URL
  und nicht per Login authentifiziert.

### Overflow und Sortierung

```
offen    = challenges.filter(state !== 'done').sortBy(sort_order)
fertig   = challenges.filter(state === 'done' && now - completed_at < 8s).sortBy(sort_order)
gepinnt  = offen.find(timer_ends_at !== null) ?? offen[0] ?? null

sichtbarOffen = [gepinnt, ...offen.without(gepinnt).take(max_visible - 1)]
sichtbarFertig = fertig.take(MAX_TOTAL_ROWS - sichtbarOffen.length)   // MAX_TOTAL_ROWS = 12
rest     = offen.length - sichtbarOffen.length
render     sichtbarOffen, dann sichtbarFertig, dann falls rest > 0 die Zeile "+{rest} weitere"
```

`max_visible` (3 bis 10) begrenzt die **offenen** Einträge. Weil in acht Sekunden theoretisch
sehr viele Einträge fertig werden können, gibt es zusätzlich ein hartes Gesamtlimit
`MAX_TOTAL_ROWS = 12` über offene und fertige Zeilen zusammen. Ohne dieses zweite Limit
wäre die Zusage "kein Scrollen" bei 10 offenen plus 30 gerade abgehakten Einträgen gebrochen.

Manuelle `sort_order` bleibt die einzige Ordnung und geht beim Abhaken und beim `reopen`
nicht verloren. Kein Scrollen, kein Paging.

## Visuelle Spezifikation

Aus `/plan-design-review`, kalibriert gegen `DESIGN.md`. Ausgangswert 3/10, nach den
sieben Durchgängen 9/10.

### Eigene Browserquelle, nicht im HUD

Das Log lebt unter `/overlay/challenges` als **zweite Browserquelle**, nicht innerhalb des
HUD-Overlays. Grund: Der OBS-Geometrievertrag aus `DESIGN.md` weist jedes Pixel der
`630 × 259`-Stage zu; der einzige unbelegte Rest misst `80 × 44 px`. Ein Log dort
unterzubringen hieße, Pet, Gruppe oder Featured Effect zu verkleinern, also ein
funktionierendes Feature für ein neues zu beschädigen.

Die neue Quelle nutzt **denselben Overlay-Token und denselben `/ws/overlay`-Socket**, sie
rendert nur eine andere Komponente. Kein zweites Auth-System für die Anzeige.

Der Streamer positioniert und skaliert sie in OBS frei. Der HUD-Vertrag bleibt bitgenau.
Maßstabsgetreues Wireframe mit allen drei geprüften Platzierungen:
`~/.gstack/projects/twitchBrudi/designs/challenge-log-20260830/placement-wireframe.html`

### Aufbau der Quelle, von oben nach unten

```
┌──────────────────────────────────────────┐
│ CHALLENGES                        2 / 7  │  Kopf: Titel (frei) + Sessionstand
├──────────────────────────────────────────┤
│ ▸  12:34                                 │  Globaler Timer, große Ziffern (optional)
├──────────────────────────────────────────┤
│ Ohne Schaden durch Zone 3   ▬▬▭  4:12    │  gepinnt (laufender Timer, sonst erste offene)
│ 10 Kills mit dem Bogen           3 / 10  │  offene, bis max_visible gesamt
│ Keine Heiltränke benutzen                │
├──────────────────────────────────────────┤
│ ✓  B̶o̶s̶s̶ ̶o̶h̶n̶e̶ ̶T̶o̶d̶ ̶b̶e̶s̶i̶e̶g̶t̶                 │  Fertig-Gruppe, verblasst nach 8 s
│ +3 weitere                               │  Überlauf, nie eine Scrollleiste
└──────────────────────────────────────────┘
```

**Zeilen-Anatomie:** Titel linksbündig und bei Bedarf mit Ellipse gekürzt, Zähler und
Timer rechtsbündig in tabellarischen Ziffern. Der Titel gewinnt bei Platzmangel nie gegen
die Zahl, weil die Zahl der veränderliche Teil ist.

**Kopfzeile.** Titel ist frei wählbar (`header_title`, Vorgabe "CHALLENGES"), damit der
Streamer sein eigenes Framing setzen kann. Rechts der Sessionstand als `erledigt / gesamt`
über das aktuelle Board. Ohne diesen Kopf wären vier Zeilen Text mit Zahlen für jemanden,
der mitten im Stream dazukommt, nicht einzuordnen.

### Globaler Timer

Optional (`global_timer_total_ms = NULL` schaltet ihn ab). Drei Zustände, kein Server-Tick
und kein Alarm, dieselbe Ableitung wie bei den Challenge-Timern:

| Zustand | `ends_at` | `paused_remain_ms` |
|---|---|---|
| läuft | gesetzt | `NULL` |
| pausiert | `NULL` | gesetzt |
| zurückgesetzt | `NULL` | `NULL` |

Fortsetzen ist `ends_at = jetzt + paused_remain_ms`. Drei Kommandos, `startGlobalTimer`,
`pauseGlobalTimer`, `resetGlobalTimer`, alle mit `commandId`-Dedupe wie die übrigen fünf.

**Darstellung:** eigene Zeile unter dem Kopf, große tabellarische Ziffern, links ein
Zustandssymbol. Pausiert zeigt Pausensymbol, ausgegraute Ziffern **und** das Wort
"pausiert", also dreifach codiert und nicht nur farblich. Unter einer Minute rot mit dem
langsamen `1.35 s`-Puls, den `DESIGN.md` bereits für kritische HP definiert. Bei null
bleibt er auf `0:00` stehen, rot und ohne Puls.

### Theming: eigene Tokens, optional überschrieben

Das Modul konsumiert seine eigenen Custom Properties und bringt eigene Werte mit. Es ist
damit ohne HUD lauffähig und sieht ohne HUD gut aus:

`--wc-surface`, `--wc-line`, `--wc-text`, `--wc-muted`, `--wc-accent`, `--wc-ok`,
`--wc-font-ui`, `--wc-font-display`, `--wc-row-height`

`wc_meta.theme_mode` steuert, was darüberliegt:

- **`inherit`** (Vorgabe in diesem Projekt): Der Host lädt eine kleine Brücken-CSS-Datei
  nach, die `--wc-*` auf die vorhandenen `--hud-*`-Werte der aktiven HUD-Variante mappt.
  Ein `trail-wood`-HUD bekommt ein Log aus Walnuss und Messing, ohne dass jemand etwas
  einstellt. Die Brücke bildet **Werte** ab, keine Sichtbarkeit, funktioniert also auch
  bei ausgeblendetem HUD.
- **`own`**: Die Brücke wird nicht geladen, das Standardtheme des Moduls steht.

Damit sind es nicht 24 Kombinationen aus vier Styles und sechs Varianten, sondern vier
**Aufbauten** in der Materialwelt, die der Streamer ohnehin gewählt hat. `ChallengeStyleId`
bestimmt ausschließlich die Struktur: `plain-list`, `plain-bullets`, `plain-numbered`,
`quest-log`. Das folgt exakt der Slot-Architektur, die `DESIGN.md` für `hud.css`
vorschreibt, eine Ebene höher.

### Lesbarkeit über beliebigem Video

Die Quelle liegt über laufendem Spiel. `wc_meta.surface_mode` schaltet zwei **gekoppelte**
Presets, nicht bloß eine Deckkraft:

- **`surface`** (Vorgabe): eigener Grund aus `--wc-surface` bei 88 % plus Haarlinie.
  Schriftgewicht 500 für Text, 600 für Titel, kein Schatten. Lesbarkeit ist garantiert.
- **`bare`**: kein Grund. Dafür Schriftgewicht mindestens 700, Schatten
  `0 1px 2px rgba(0,0,0,.9), 0 0 6px rgba(0,0,0,.7)`, und die Mindestgröße steigt um zwei
  Stufen, weil konturierter Text in klein zu Matsch wird. Fortschrittsbalken und
  Haarlinien bekommen denselben Schatten, sonst verschwinden sie während der Text bleibt.
  Der Puls des globalen Timers nutzt denselben Halo.

Die Kopplung ist der Punkt: Die Fläche einfach abzuschalten, ohne die Schrift mitzuändern,
erzeugt genau den unlesbaren Fall. **`surface` garantiert Lesbarkeit, `bare` maximiert sie
ohne Garantie.** Das gehört so in die Doku.

### Zustände

| Fläche | Loading | Empty |
|---|---|---|
| Overlay-Quelle | Inhalt blendet in 160–240 ms ein, kein Springen | **vollständig transparent**, auch kein Kopf |
| Admin-Board | Skelett der Zeilen | Text plus Primäraktion "Erste Challenge anlegen" |
| Live-Seite | Skelett der Zeilen | Text plus Verweis auf das Board |

Die Overlay-Quelle folgt damit der bestehenden Regel aus `DESIGN.md`, dass ein Overlay
ohne gültigen Zustand keine Fehlerfläche rendert, sondern transparent bleibt. Ein leerer
Kasten im Bild wäre Dekoration ohne Information.

### Barrierefreiheit

- **Fertig ist nie nur Farbe.** Häkchen **plus** Durchstreichung **plus** Farbe, in allen
  vier Styles.
- **Pausiert ist nie nur Farbe.** Symbol plus Helligkeit plus Wort.
- **Sortieren im Board** funktioniert zusätzlich per Pfeiltasten mit sichtbarem Fokus,
  nicht nur per Ziehen mit der Maus.
- **Touch-Ziele** 44 px für `+` und `−`, 38 px für alles Übrige.
- `prefers-reduced-motion` entfernt Einblenden, Puls und Zeremonie.

### Einrichtungsseite

Der Challenges-Workspace bekommt einen Abschnitt, der **alle drei OBS-Quellen** an einer
Stelle zeigt: HUD-Overlay, Challenge-Log, Live-Bedienseite. Je Zeile die URL mit
Kopierknopf, die empfohlene Größe, und ein Satz wozu sie dient. Die Live-Seite bekommt
zusätzlich einen QR-Code für den Handy-Weg und den Menüpfad für den OBS-Weg.

Durch die Entscheidung für eine eigene Browserquelle hat der Streamer drei
Einrichtungsschritte statt einem. Jeder davon ist eine Stelle, an der jemand aufgibt. Für
ein Modul, das fremde Streamer benutzen sollen, ist Einrichtungsreibung der wirksamste
Grund, es nicht zu benutzen. Deshalb ist diese Seite kein Nice-to-have.

### Typografie und Maße

Aus `DESIGN.md` übernommen, nicht neu erfunden: Atkinson Hyperlegible Next für Lesetext,
Georgia für Kopf und Display, tabellarische Ziffern für alle Zähler und Countdowns,
4-px-Basisraster mit Abständen 8/12/16, Zeilenhöhe an der Effektreihe orientiert (33 px),
Mindestgröße 10 px im `surface`-Modus. Keine Default-Font-Stacks.

## Architektur-Dokumentation ist Teil dieser Änderung

`docs/ARCHITECTURE.md` wird im selben Schritt nachgezogen:

1. Das ASCII-Diagramm bekommt die Challenges und den dritten Socket-Typ.
2. Der Abschnitt "Veröffentlichungsgrenze" beschreibt heute Draft, Save und
   Konfliktbehandlung als *das* Modell des Systems. Nach dieser Änderung gibt es **zwei**:
   das HUD mit Read-Modify-Write und Konfliktdialog, die Challenges mit deduplizierten
   Kommandos. Beide gehören benannt, mit der Begründung, warum ein Häkchen mitten im Stream
   niemals einen Konflikt werfen darf.

Das ist der teuerste Teil dieses Plans und gehört aufgeschrieben statt später beim
Debuggen entdeckt.

**`DESIGN.md` wird ebenfalls im selben Schritt nachgezogen.** Das Dokument schließt mit
der eigenen Regel: *"Jede Änderung an Tokens, HUD-Geometrie oder Interaktionsmustern muss
dieses Dokument und die betroffenen Komponenten-/Browserprüfungen gemeinsam
aktualisieren."* Diese Änderung führt eine zweite Overlay-Quelle, einen eigenen
Token-Vertrag und zwei neue Interaktionsmuster ein. Nachzutragen sind:

1. Die zweite Browserquelle `/overlay/challenges` und die Feststellung, dass der
   `630 × 259`-Geometrievertrag von ihr **nicht** berührt wird.
2. Der `--wc-*`-Token-Vertrag des Moduls und die Brücke auf die `--hud-*`-Werte.
3. Die zwei gekoppelten `surface_mode`-Presets samt der ehrlichen Einordnung, dass nur
   `surface` Lesbarkeit garantiert.
4. Der `1.35 s`-Puls bekommt einen zweiten Anwendungsfall neben kritischer HP.

## Free-Tier-Budget

Free-Grenzen: 5 Mio. gelesene und 100.000 geschriebene Zeilen pro Tag für SQLite-DOs, ein
separates Limit von **100.000 DO-Requests pro Tag** und ein Duration-Budget. Deletes zählen
als Writes, Indexänderungen als zusätzliche Row-Writes.

Untergrenze der Schreibvorgänge, **ohne** Index- und Session-Writes:

| Aktion | Writes (Untergrenze) |
|---|---|
| Jede Mutation (`increment`, `complete`, `reopen`, `startTimer`, `stopTimer`) | 3 (Challenge, `wc_commands`, `event_seq`) |
| Board-Save mit N Challenges | abhängig vom Upsert-Algorithmus, mindestens N + 1 |
| Settings-Save | 1 |

Dazu bei **jeder** authentisierten Aktion Writes außerhalb des Moduls, insbesondere
`editor_sessions.idle_expires_at` (`src/channel/channel-object.ts:1270`).

Die Größenordnung trägt: einige hundert Live-Kommandos pro Stream liegen zwei
Größenordnungen unter der Tagesgrenze, und der **Request-Zähler ist die engere Grenze** als
der Write-Zähler. Genau deshalb ist `DOCK_TOKEN_LIMITER` keine Kür.

**Verbindlich wird das Budget erst aus echten `rowsWritten`-Messungen** der tatsächlichen
SQL-Statements, inklusive Index-Kosten des `TEXT PRIMARY KEY`.

## Test Coverage Plan

QA-Artefakt:
`~/.gstack/projects/twitchBrudi/sb-worktree-audit-broadcast-eng-review-test-plan-20260830-181500.md`

```
CODE PATHS                                                  USER FLOWS
[+] contracts/predicates.ts                                 [+] Challenges anlegen und zeigen
  └── isChallengeTitle / isCount / isInstant                  ├── [→E2E] Anlegen im Board → im Overlay sichtbar
      ├── CRITICAL Grenzwert-Tabelle durch BEIDE Aufrufer     ├──        clientId → id Zuordnung in der Antwort
      │        (Zod-Schema + wire.ts), identisches            ├──        Sortierung ziehen → Overlay folgt
      │        Accept/Reject erzwungen                        └──        Style wechseln → Chunk lädt nach
      └── Emoji, NFC-Formen, Leerstring, Maximum+1
                                                            [+] Live-Bedienung im Dock
[+] domain/                                                   ├── [→E2E] +1 → Overlay zählt hoch
  ├── applyIncrement()                                        ├──        Ziel erreicht → genau eine Zeremonie
  │   ├── positiv / negativ / ohne Wirkung                    ├──        Doppeltipp → zählt einmal
  │   ├── Klemmen bei 0 und bei target_count                  ├──        Timer starten → Countdown läuft
  │   └── Auto-Complete feuert, kein doppeltes Event          └──        Fehler → Wert rollt zurück
  ├── applyComplete / applyReopen / Timer-Kommandos
  │   └── Übergangstabelle vollständig                      [+] Zwei Admins gleichzeitig
  ├── mergeDefinition()   CRITICAL                            ├── [→E2E] Beide +1 → Summe, nicht Overwrite
  │   ├── Timer entfernt während er läuft → state pending     ├──        Board-Save währenddessen → Zähler bleibt
  │   ├── Timerdauer geändert während er läuft → ends_at      ├──        Board-Save + Settings-Save parallel →
  │   │        bleibt                                         │           kein gegenseitiger Konflikt
  │   ├── targetCount null → count bleibt gespeichert         └──        Gelöscht während Tipp → not_found sichtbar
  │   └── targetCount sinkt unter count → geklemmt
  ├── deriveExpired()                                       [+] Störungen
  │   └── kein Timer / Zukunft / Vergangenheit / done         ├──        Reconnect mitten in Vollendung →
  ├── selectVisible()                                         │           keine nachträgliche Zeremonie
  │   ├── gepinnt = laufender Timer, sonst erster             ├──        Verbindung weg → Log weg, HUD bleibt
  │   ├── max_visible begrenzt NUR offene                     ├──        Parse-Fehler → Log weg, HUD flackert NICHT
  │   ├── MAX_TOTAL_ROWS greift bei vielen Fertigen           ├──        Dock-Token rotiert → Dock-Socket schließt
  │   ├── Fertig-Gruppe nur < 8 s                             └──        Rate-Limit greift → rate_limited im Dock
  │   └── "+N weitere" ab Überlauf
  └── normalizeSortOrder() lückenlos 0..N-1                 [+] Aussehen
                                                              ├── plain-list / -bullets / -numbered / quest-log
[+] service/commands.ts                                       └── effects_enabled aus → keine Zeremonie,
  ├── CRITICAL Dedupe für ALLE fünf Mutationen:                      kein Sound, reduced-motion respektiert
  │        complete→reopen→complete-Retry vollendet
  │        NICHT erneut; startTimer→stopTimer→
  │        startTimer-Retry startet KEINEN neuen Timer
  ├── Replay gleicher Hash → replayed:true
  ├── Replay anderer Hash → idempotency_mismatch
  ├── Kommando ohne Wirkung bumpt event_seq NICHT
  └── Kommando auf gelöschte Challenge → not_found

[+] adapters/sqlStorageRepository.ts
  ├── CRITICAL Board-Save: Revision lesen, prüfen, schreiben im SELBEN transactionSync
  ├── CRITICAL Board-Save schreibt keine Laufzeitfelder
  ├── board_revision und settings_revision sind unabhängig
  ├── Migration läuft als MIGRATION_3 im vorhandenen Ledger
  └── Prune löscht wc_commands älter als 24 h

[+] Host: worker/index.ts, channel-object.ts
  ├── 6 Routen: Auth, CSRF, Same-Origin, 32-KiB-Limit
  ├── CRITICAL Dock-Token: Hash, Rotation, Widerruf, aktives Schließen der Sockets,
  │        Scope NUR auf /api/challenges/commands und /ws/dock
  ├── CRITICAL DOCK_TOKEN_LIMITER greift auf Route und Socket-Upgrade
  ├── revision_conflict trägt den Snapshot im Body
  ├── challenge_update geht an editor, overlay UND dock
  └── channel_state.revision bleibt unberührt

[+] Host: overlay/wire.ts, OverlayApp.tsx, main.tsx
  ├── handgeschriebener Parser über predicates.ts
  ├── Parse-Fehler blendet nur das Log aus, kein Reload
  ├── /live/challenges hat einen eigenen Zweig in main.tsx
  └── 120-KiB-Gate gemessen, dynamische Style-Chunks bestätigt

COVERAGE: 0/56  |  CRITICAL: 7  |  E2E: 4
```

Testdateien nach bestehender Konvention: `tests/unit/domain/`, `tests/unit/contracts/`,
`tests/components/admin/`, `tests/worker/channel.test.ts`, `tests/e2e/`.

## Open Questions

- **Board-Save-Algorithmus:** Upsert plus Delete oder Delete-all plus Insert-all? Entscheidet
  die Schreibkosten, muss gemessen werden.
- **Audio-Maximalgröße** für das erweiterte Transfer-Gate.
- **Zweiter Konsument:** Solange keiner benannt ist, bleibt die Modul-Grenze eine begründete
  Vermutung.

## Reviewer Concerns (offen)

- **Der Uhr-Drift im Overlay ist eingeordnet, nicht gelöst.** Er betrifft heute schon die
  Effekt-Timer. Ein Offset-Handshake wäre ein eigenes, lohnendes Vorhaben.
- **Das Free-Tier-Budget ist geschätzt, nicht gemessen.**
- **`replayed: true` gibt den aktuellen, nicht den damaligen Stand zurück.**
- **Die Outside Voice hält die Schichtung für zu viel** für einen einzigen Adapter und
  hostgebundene UIs. Der Einwand ist fair, die Modul-Grenze wurde zweimal bewusst bestätigt.

## Success Criteria

- Ein `+1` mitten im Stream schlägt nie mit einem Konflikt fehl, auch nicht während ein
  Board-Save, ein Settings-Save oder ein HUD-Save läuft.
- Ein Board-Save kann einen Zähler nicht zurückdrehen.
- Ein Board-Save und ein Settings-Save behindern sich nicht gegenseitig.
- Zwei Admins, die gleichzeitig inkrementieren, ergeben die Summe.
- **Jede** Mutation doppelt gesendet wirkt einmal, auch `complete` und `startTimer` nach
  einem zwischenzeitlichen Zustandswechsel.
- Eine Zeremonie feuert bei jeder Live-Änderung genau einmal und nach einem Reconnect gar
  nicht nachträglich.
- Kein Alarm, kein serverseitiger Tick.
- Server und Overlay akzeptieren identische Eingaben. Der Grenzwerttest beweist es.
- Eine kaputte Challenge-Nachricht lädt das Overlay NICHT neu.
- Der Dock-Token öffnet ausschließlich `/api/challenges/commands` und `/ws/dock`, und er
  wird rate-limitiert.
- Der Dock erhält beim Verbinden sofort einen vollständigen Snapshot.
- Das Overlay scrollt nie, auch nicht bei 30 gleichzeitig abgehakten Einträgen.
- Die Modultransaktion schreibt ausschließlich in `wc_`-Tabellen. (Der umgebende Request
  fasst weiterhin `editor_sessions` an, bestehendes Verhalten.)
- Alle Build-Gates bleiben grün, inklusive des um Audio erweiterten Transfer-Gates.

## NOT in scope

- **Stream Quest Director** (Runs, Quest-Ketten, Bonusziele, VOD-Recap). Eigenes Produkt.
- **`failed`-Zustand und Fehlschlag-Zeremonie.** Ohne Alarm gibt es keinen garantierten
  Moment, in dem sie feuern könnten.
- **Eigener Migrations-Ledger für das Modul.** Erst beim zweiten Konsumenten.
- **Konfigurierbares Tabellenpräfix nach außen.** V1 nutzt die Konstante `wc_`.
- **Offset-Handshake für die Overlay-Uhr.** Eigenes Vorhaben, verbessert auch die
  bestehenden Effekt-Timer.
- **Chat-Kommandos und Stream-Deck-Anbindung.** Die Event-Schnittstelle hält den Weg offen.
- **Offline-Cache für Challenges.** Bewusst nicht.
- **Echte npm-Paketierung des Moduls.** V1 liefert Code-Organisation.
- **Änderungen an den OAuth-Flüssen.** Entfallen durch den Dock-Token.
- **Mehrere Dock-Tokens gleichzeitig.** Ein Singleton wie beim Overlay-Token reicht.

## What already exists (und wird wiederverwendet)

| Vorhanden | Wo | Wird genutzt für |
|---|---|---|
| Socket-Broadcast mit Session-Prüfung | `channel-object.ts` `sendToSockets` | `challenge_update` an drei Socket-Arten |
| Token-Socket mit Subprotokoll und Limit | `/ws/overlay`, `channel-object.ts:976` | `/ws/dock` |
| Token mit Hash, Envelope, Generation, Rotation, aktivem Schließen | `overlay_tokens`, `channel-object.ts:701` | `wc_dock_tokens` |
| Rate-Limit-Bindings | `wrangler.jsonc`, `index.ts:570` | `DOCK_TOKEN_LIMITER` |
| Absoluter Ablaufzeitpunkt, Client rechnet | `activeEffect.expiresAt` | `timer_ends_at` |
| Pruning nach Alter und Anzahl | `pruneHistoryAndAudit` | `wc_commands` |
| Singleton-Tabellenmuster | `channel_state`, `overlay_tokens` | `wc_meta`, `wc_dock_tokens` |
| Linearer Migrator | `migrations.ts` | `MIGRATION_3` |
| Explizite Routen-Map | `worker/index.ts:522` | sechs neue Routen |
| Pfad-Dispatch mit Code-Splitting | `main.tsx` | `/live/challenges`, Style-Chunks |
| Synchrone SQLite-Transaktion | `channel-object.ts:585` | atomare Kommandos |
| Handgeschriebener Parser | `wire.ts` | `challenge_update` |
| Konfliktcode und Fehlerkatalog | `API_ERROR_CODES` | Board, Settings, Idempotenz |
| Watchdog-Muster mit Marker | `OverlayApp.tsx:21` | bewusst NICHT erweitert |

Nichts davon wird nachgebaut.

## Next Steps

Die Reihenfolge folgt den Abhängigkeiten. Der erste sichtbare Ende-zu-Ende-Moment liegt bei
Schritt 4 und läuft über die vorhandene Admin-Session, nicht über den Dock. Der Dock kommt
später, weil er ein eigenes Token- und Socket-System braucht.

1. `contracts/predicates.ts` und `domain/` als reine, testbare Logik, inklusive
   `mergeDefinition` und `selectVisible`. Dazu die CRITICAL-Grenzwerttabelle durch beide
   Aufrufer. Keine Abhängigkeit nach außen, vollständig unit-testbar.
2. `MIGRATION_3` im vorhandenen Ledger, `ChallengeRepository`, DO-SQLite-Adapter. Dabei
   `rowsWritten` der echten Statements messen und das Free-Tier-Budget festschreiben.
3. Kommando-Service in `transactionSync` mit Dedupe für alle fünf Mutationen, plus die
   vier Session-Routen (GET, commands, board, settings).
4. `challenge_update` auf den vorhandenen Editor- und Overlay-Sockets, handgeschriebener
   Overlay-Parser, `plain-list` im Overlay, Log-Ausblenden bei Parse-Fehler.
   **→ erster sichtbarer Ende-zu-Ende-Moment, bedient über die Admin-Session.**
5. Admin-Shell mit Workspace-Switcher plus Board mit getrennten Definitionsfeldern und
   `clientId`-Zuordnung beim Anlegen.
6. `wc_dock_tokens`, die beiden Token-Routen, `/ws/dock`, `DOCK_TOKEN_LIMITER`, aktives
   Schließen bei Rotation. Inklusive Scope-Test.
7. `/live/challenges` als dritter Zweig in `main.tsx`, optimistisch mit Rückrollen,
   Touch-Ziele 44/38 px, Regel unter 280 px.
8. Globaler Timer: drei Felder in `wc_meta`, drei Kommandos mit Dedupe, eigene Zeile im
   Overlay mit `1.35 s`-Puls, Start/Pause auf der Live-Seite, Reset nur im Admin.
9. Theming: `--wc-*`-Vertrag, Standardtheme des Moduls, Host-Brücke, `theme_mode`.
   Dazu `surface_mode` mit den zwei gekoppelten Presets.
10. Transfer-Gate um Audio erweitern, Style-CSS auf dynamische Chunks umstellen und das
    `Math.max` im Gate gegen den echten Netztransfer verifizieren.
11. Zeremonie-Registry beim Host, Aus-Schalter, `prefers-reduced-motion`.
12. Einrichtungsseite im Challenges-Workspace mit allen drei Quellen, Kopierknöpfen und
    QR-Code.
13. `plain-bullets`, `plain-numbered` und `quest-log` mit eigenen Assets und eigenem Sound.
14. `docs/ARCHITECTURE.md` **und `DESIGN.md`** nachziehen.

## What I noticed about how you think

- *"überleg dir was clevers, vor allem wegen der datenbank - dazu wäre ja okay, aber dann
  mit explizitem tabellenprefix"* — du hast nach dem Cleveren gefragt und im selben Satz
  die Antwort schon eingegrenzt.
- *"die effekte sollen aber abschaltbar sein, weil wie gesagt ja nicht alles wow ist"* — du
  hast den Entwurf mitten in der Session korrigiert, nicht abgenickt. Codex hat unabhängig
  genau diesen Satz als das Aufschlussreichste markiert, was du gesagt hast.
- *"und nicht in der eh schon zu langen rechten admin-oberfläche"* — du hast beim
  Spezifizieren eines neuen Features ein Problem der bestehenden Oberfläche benannt.
- *"denk dran dass wir im cloudflare free bleiben wollen!"* — kam nach der
  Architekturentscheidung und hat trotzdem zwei Punkte gekippt. Ohne diesen Satz stünde
  heute ein Durable-Object-Alarm im Plan.

## Approved Mockups

| Screen/Section | Pfad | Richtung | Notizen |
|---|---|---|---|
| Platzierung auf der OBS-Fläche | `~/.gstack/projects/twitchBrudi/designs/challenge-log-20260830/placement-wireframe.html` | Eigene Browserquelle, HUD-Vertrag unangetastet | Maßstabsgetreu 1:1. Zeigt alle drei geprüften Platzierungen und warum A und B verworfen wurden. Ist eine Platzierungsstudie, **keine** visuelle Gestaltung des `quest-log`-Styles. |

Der gstack-Designer war in dieser Session nicht nutzbar (kein OpenAI-Key hinterlegt),
deshalb entstand das Wireframe von Hand statt als KI-Mockup. Die visuelle Ausarbeitung des
`quest-log`-Styles steht noch aus.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 21 issues, 0 critical gaps, 56 test paths mapped |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | score: 3/10 → 9/10, 8 Entscheidungen |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**CROSS-MODEL:** Vier unabhängige Läufe (zwei Codex-Spec-Reviews auf Revision 1 und 2, ein
Codex-Outside-Voice auf Revision 4, plus dieses Eng-Review). Die Modelle waren sich bei
"kein Scrollen im Overlay", "kein Durable-Object-Alarm" und "eingebettete Feature-Slice
statt eigenem Durable Object" einig. Codex hat drei Blocker gefunden, die dieses Review
übersehen hatte, darunter einen, den dieses Review selbst erzeugt hat: Der auf Token-Auth
umgestellte OBS-Dock hätte keinen WebSocket und damit keine Wahrheitsquelle für das
optimistische Rückrollen gehabt. Umgekehrt hat dieses Review zwei Dinge gefunden, die
Codex nicht sah: die Wiederkehr des dokumentierten Validator-Drifts über die Modul-Grenze
und die verlorene Anforderung nach Aufzählungspunkten. Ein Einwand von Codex wurde
bewusst nicht übernommen: die Kritik an der Schichttiefe der Modul-Grenze, weil die
Extrahierbarkeit das erklärte Ziel des Vorhabens ist und zweimal bestätigt wurde.

**DESIGN:** Startwert 3/10. Die Ursache war strukturell, nicht kosmetisch: Der Plan
beschrieb ein Overlay-Feature, ohne dass es im `630 × 259`-Geometrievertrag Platz gab, und
schwieg zu Typografie, Zuständen, Barrierefreiheit und dem Verhältnis zu den sechs
HUD-Varianten. Acht Entscheidungen später steht er bei 9/10. Die folgenreichste war,
das Log gar nicht ins HUD zu setzen, sondern als eigene Browserquelle. Die zweitfolgenreichste
kam vom Nutzer als Korrektur: Das Modul muss sein eigenes Theming mitbringen, statt vom HUD
zu erben, weil das HUD abschaltbar ist und in einem Zielprodukt gar nicht existiert.

**VERDICT:** ENG + DESIGN CLEARED — ready to implement

NO UNRESOLVED DECISIONS
